import mDot from './mDot';
import mvAdd from './mvAdd';
import lrelu from './lrelu';
import cost from './cost';

const op = {
  mDot, mvAdd, lrelu, cost
};

export {
  op as default,
  mDot, mvAdd, lrelu, cost
}
