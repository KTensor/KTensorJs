import t from 'tensor';
import n from 'node';
import c from 'context';
import op from 'operation';

const run = (node, variables)=>{

};

const K = {
  t, n, c, op
};

export {
  K as default,
  t, n, c, op
};
