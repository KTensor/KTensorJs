import t from 'tensor';
import n from 'node';
import c from 'context';
import m from 'math';

const run = (node, variables)=>{

};

const K = {
  t, n, c, m
};

export {
  K as default,
  t, n, c, m
};
