import t from 'tensor';
import n from 'node';

const dot = (matrix_1, matrix_2)=>{
  return matrix_1;
};

const op = {
  dot
};

const run = (node, variables)=>{

};

const K = {
  t, n
};

export {
  K as default,
  t, n
};
