import t from 'tensor';
import n from 'node';
import c from 'context';

const dot = (matrix_1, matrix_2)=>{
  return matrix_1;
};

const op = {
  dot
};

const run = (node, variables)=>{

};

const K = {
  t, n, c
};

export {
  K as default,
  t, n, c
};
